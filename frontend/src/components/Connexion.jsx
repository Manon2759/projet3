import React from 'react';

const Connection = () => {
    return (
        <div className='connection'>

            <button className='connection_button'>CONNEXION</button>

        </div>
    );
};

export default Connection;